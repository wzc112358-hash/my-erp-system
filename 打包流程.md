我的项目情况如下：

前端(frontend文件夹)：
    技术栈：Vite + React + TypeScript
    包管理：npm
    构建命令：npm run build
    构建输出目录：dist

后端(backende文件夹)：
    使用 PocketBase
    项目包含 Go hooks
    有 main.go、go.mod、go.sum
    有 hooks 文件夹
    有 pb_data 和 pb_migrations
    有pocketbase文件
    需要通过 Go 编译 PocketBase

开发环境：
    WSL
    Linux 发行版：Ubuntu
    Docker 已安装
    docker compose 已安装

最终部署目标（未来,现阶段暂时不用完成）：
    Linux Debian 云服务器
    使用 Traefik 反向代理
    域名访问前端
    api 子域名访问 PocketBase

现在只需要完成 本地 Docker 打包和测试运行，暂时不用配置 HTTPS。

请你一步一步帮我完成 本地 Docker 打包与运行测试（每一步做完都需要让我进行确认）。

一，编写 React 前端 Dockerfile

要求：
1）使用 node 构建
2）执行 npm install
3）执行 npm run build
4）使用 nginx 作为运行环境
5）将 dist 目录复制到 nginx
6）监听 80 端口

同时解释每一行 Dockerfile 的作用。


二，编写 PocketBase Dockerfile

由于项目有 Go hooks，需要：
1）使用 golang 镜像构建
2）编译 main.go
3）生成 PocketBase 可执行文件
4）运行 pocketbase serve
5）监听 8090 端口

同时需要：
保留 pb_data 目录
通过 Docker volume 挂载数据目录


三，编写 docker-compose.yml

要求包含三个服务：
1）frontend
2）pocketbase
3）traefik

traefik 只需要简单配置：
监听 80 端口
启用 docker provider
打开 dashboard（8080）

frontend：
build ./frontend
暴露 80
通过 traefik router

pocketbase：
build ./pocketbase
暴露 8090
挂载 pb_data
通过 traefik router


四，构建 Docker 镜像
指导我在 WSL 终端运行：
docker compose build
解释构建过程中每个阶段在做什么。


五，本地运行并测试

运行：
docker compose up

验证以下内容：
1）React 前端可以访问
http://localhost
2）PocketBase API 可以访问
http://localhost:8090
3）Traefik dashboard
http://localhost:8080

同时给出：
docker ps
docker logs
docker compose down

等常用调试命令。